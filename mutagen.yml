sync:
  defaults:
    ignore:
      vcs: false
      paths:
        - .DS_Store # macOS files
        - .idea
    permissions:
      defaultFileMode: 644
      defaultDirectoryMode: 755
  loggingbundle:
    alpha: "."
    beta: "docker://root@swoole-zmq-php74/srv/www"
